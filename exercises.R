#create a vector in R using c function
#create a numeric vector 0 to 10 values
#how to get vector length in R
#add element to vector in R
#how to check NA values from a vector
#how to remove NA values from above vector
#how to create a data frame from vectors
#how to create list in R
#how to convert vector to list in R
#how to sort vector in R
#how to remove values from R vector
#create a empty data frame
#select rows from above data frame
#select columns from data frame
#replace a column values from data frame
#how to remove columns from data frame
#add column to data frame
#sort the data frame using single column
#sort the data frame using multiple column
#how to check the NA from data frame
#replace NA values with 0
#how to check the duplicate values from the data frame
#how to remove the duplicate rows from the data frame
#filter the data set using multiple conditions
#how to create a new column using mutate function
#how to create a new column using $ symbol
#how to print first 6 rows from data frame
#how to print last 4 rows from data frame
# how to check dimension of the data frame
# how to check no of columns from the data frame
#print hte column names from  data frame
# print the number of rows from the dataframe
# remove the wanted column from the dataframe
#create a new column using ifelse function
